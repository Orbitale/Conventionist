<h1>Conventionist</h1>

<h2>{{ 'registration.email.title'|trans(locale: locale) }}</h2>

{{ 'registration.email.body'|trans({
    "%signedUrl%": signedUrl,
    "%expiration%": expiration,
}, locale: locale)|raw }}
